---
title: "指令实现"
tags:
  - 学习/英语/词汇
  - 指令
  - 内部规程
date: 2026-04-20
---

# 指令实现

> [!danger] 本文档仅供 AI 内部参考
> 用户可见的交互规则见 [[交互指令]]。本文档是 AI 执行指令时的详细操作规程。

---

## Rule 1：摄入（Ingest）

### 触发条件

- `/摄入 <单词>`
- `学习单词 <单词>`
- `摄入 <单词1>, <单词2>, ...`（批量，逗号分隔）

### 执行流程

#### Phase 1：信息采集

对每个单词执行：

1. **联网查询**（WebSearch）：
   - 查询 `<单词> 音标 词频 考研` + `<单词> 词根词缀 词源` + `<单词> 考研真题 例句 搭配`
   - 搜索预算：每词最多 3 次搜索

2. **本地检查**：
   - 读取 `Wiki/_模板-单词笔记.md` 获取模板结构
   - 搜索 `Wiki/词根词缀/` 检查词根笔记是否已存在
   - 搜索 `Wiki/同义辨析/` 检查同义群笔记是否已存在
   - 搜索 `Wiki/形近词/` 检查形近词笔记是否已存在
   - 搜索 `Wiki/L0_单词集合/` 检查单词是否已存在

#### Phase 2：内容生成（L0-L4 完整填充）

按模板顺序填充：

**L0 — 基础信息**（frontmatter）
- `title`, `tags`, `date`, `mastery: L0`, `word_freq`, `semantic_field`, `prototype`, `extension_dim`, `last_review`, `review_count: 0`
- **新增字段**：`phonetic`（音标）、`pos`（主词性）、`metaphor_type`（隐喻类型）、`word_root`（核心词根）、`network_activation`（网络激活列表）

**语义场判定规则**：

| 关键词特征 | 归属语义场 |
|-----------|-----------|
| 科学/逻辑/数据/证据/理论 | 理性世界 |
| 情感/品质/态度/欲望/恐惧 | 人本世界 |
| 文化/传统/日常/家庭/食物 | 社会习俗 |
| 学术/职业/制度/教育/技术 | 社会专业 |
| 商业/金融/贸易/市场/利润 | 资本经济 |
| 法律/政治/治理/权利/政策 | 政治法律 |
| 自然/地理/天气/环境/动物 | 自然物理 |
| 身体/动作/感知/疾病/医疗 | 人体动作 |
| 器物/工具/建筑/场所/材料 | 物品工具 |
| 介词/连词/副词/关系词/代词 | 抽象关系 |

**L0 — 核心释义**
- 格式：`**词性.** ①{***重点义项***}；②义项；`
- 仅列考研常考义项，不超过 4 个

**L0 — 原型义**
- 原型义 + 延伸维度 + 隐喻类型（方位/结构/本体/无）

**L0 — 词根词缀**
- 格式：`<前缀>(含义) + [[词根]](含义) + <后缀>(含义)`
- 叙事记忆：一句话故事，遵循5标准

**L1 — 词义链路**
- 原型义 → 引申义 + 激活条件
- 统筹模式（6选1-2）
- 验证 checklist（可逆/可统筹/有支撑）

**L1 — 词性转换** + **L1 — 记忆锚点**

**L2 — 同义词辨析 + 反义词 + 派生词链接**
- 同义词辨析：四维表格 + "增"标记 + `[[已有辨析笔记]]` 链接（如存在）
- 反义词：`[[wikilink]]` + 辨析说明
- 派生词链接：表格（派生词/构词分析/释义/链接关系）

**L3 — 搭配与短语** + **L3 — 真题/语料关联**
- 联网获取真题例句，标注来源

**L4 — 主动产出**
- 留空，仅在用户实际产出后通过 `/深化` 填写

#### Phase 3：保存 + 网络激活选项

1. 写入 `Wiki/L0_单词集合/<单词>.md`
2. 如文件已存在 → 提示用户并询问是否覆盖
3. 输出摘要：
```
✅ <单词> 已摄入
- 语义场：<语义场> | 词频：<高频/中频/低频>
- 原型义：<原型义>
- 词根：<拆分>
- ⚠️ 建议：创建词根笔记 [[词根]]
```
4. **展示网络激活选项**（列出该单词可贡献到的 Layer 2 网络文件）：
```
🌐 <单词> 的网络激活选项：
  [1] 词根 <root>（<含义>）→ 参与生成词根笔记 [[<root>]]
  [2] 同义群 <syn1>/<syn2>/<syn3> → 参与生成辨析笔记
  [3] 形近词 <list> → 参与生成形近词笔记

回复编号激活（如 "123" 或 "全选" 或 "跳过"）：
```
5. 解析用户回复，将激活选择写入 frontmatter 的 `network_activation` 字段（如 `network_activation: [词根, 同义辨析, 形近词]`），供 `/回顾` 时读取该字段决定是否纳入对应网络文件的生成。

### 错误处理

- 搜索无结果 → 使用自身知识，标注"未联网验证"
- 词根无法拆分 → 词根词缀 section 留空，在记忆锚点中使用词拆分记忆
- 语义场无法判定 → 默认"抽象关系"，提示用户手动调整

---

## Rule 2：深化（Deepen）

### 触发条件

- `/深化 <单词>`
- 用户提供对已有笔记的修正意见

### 执行流程

1. 读取 `Wiki/L0_单词集合/<单词>.md`
2. 解析用户意图，确定需要修改的 section
3. 执行修改（修正/补充/删除）
4. 更新 frontmatter（mastery / last_review / review_count）
5. **级联检查**：如修改了词根词缀 section，检查对应的 Layer 2 词根笔记是否需要同步更新
6. 保存并输出修改摘要

---

## Rule 3：查询（Query）

### 触发条件

- `/查询 <问题>`
- 自然语言提问（关于词汇知识库内容）

### 执行流程

1. 读取 `词汇知识库索引.md`，定位相关笔记
2. 读取相关单词笔记 + Layer 2 网络笔记
3. 基于知识库内容回答，优先引用笔记内容
4. 如知识库中无相关信息 → 明确告知并建议 `/摄入`
5. **不写文件**

---

## Rule 4：复习（Review）

### 触发条件

- `/复习`（全部）| `/复习 L0` | `/复习 L1`

### 执行流程

1. **筛选**：读取所有 `L0_单词集合/` 下的笔记，按 `last_review` 筛选到期待复习的单词
   - 复习间隔：L0→L1: 1天 / L1→L2: 3天 / L2→L3: 14天 / L3→L4: 60天
2. **逐词回忆**：展示原型义，等待用户回忆
3. **核对**：展示完整笔记
4. **更新**：`last_review` = 当天，`review_count` +1

---

## Rule 5：回顾 + 生成网络（Review & Generate）

### 触发条件

- `/回顾`（全部类型）| `/回顾 词根` | `/回顾 同义辨析` | `/回顾 形近词`

### 执行流程

#### Step 1：扫描 Layer 1

读取 `Wiki/L0_单词集合/` 下所有单词笔记，提取以下信息：

**词根提取**：
- 扫描每个笔记的"词根词缀" section
- 提取词根名称（如 `ambi`、`gest`、`rupt`）
- 按词根分组，收集所有使用该词根的单词

**同义群提取**：
- 扫描每个笔记的"同义词辨析" section
- 提取同义词列表（如 `[[vague]]`, `[[obscure]]`, `[[implicit]]`）
- 按同义群分组（多个单词引用同一组同义词 → 属于同一群）

**形近词提取**：
- 扫描每个笔记中提到的形近词
- 按形近群分组

#### Step 2：生成/更新 Layer 2 文件

**词根笔记**（`Wiki/词根词缀/<词根>.md`）：
- 如文件已存在 → 追加新发现的派生词到"派生词族"表格
- 如文件不存在 → 使用 `_模板-词根笔记.md` 创建，填充：
  - 核心含义：从各单词笔记的词根拆分中提取
  - 叙事记忆：从各单词笔记的叙事中提取最佳版本
  - 派生词族：汇总所有使用该词根的单词，提取各自的构词分析和叙事
  - 建立双向链接：词根笔记 `[[单词]]` ↔ 单词笔记 `[[词根]]`

**同义辨析笔记**（`Wiki/同义辨析/<主题>.md`）：
- 如文件已存在 → 追加新发现的同义词到辨析矩阵
- 如文件不存在 → 使用 `_模板-同义辨析笔记.md` 创建，填充：
  - 辨析矩阵：从各单词笔记的同义词 section 中提取四维对比
  - 用法对比：从各单词笔记的搭配/语料 section 中提取例句
  - 建立双向链接

**形近词笔记**（`Wiki/形近词/<主题>.md`）：
- 同上逻辑

#### Step 3：建立双向链接

- Layer 2 文件中：`[[ambiguity]]`, `[[ambiguous]]`, `[[ambition]]` ...
- Layer 1 文件中：确认 `[[ambi]]` 链接已存在（如用户在摄入时激活了该链接）

#### Step 4：输出摘要

```
✅ 回顾完成
- 词根：新增 2 个（ambi, gest），更新 1 个（rupt）
- 同义辨析：新增 1 个（模糊的），更新 2 个
- 形近词：新增 1 个
- ⚠️ 建议：人工精修新增的网络文件
```

---

## Rule 6：巡检 + 维护（Lint & Maintain）

### 触发条件

- `/巡检`

### 执行流程

#### A. 确定性检查（自动修复）

1. **链接完整性**：
   - 扫描 `L0_单词集合/*.md` 和 `词根词缀/*.md` 和 `同义辨析/*.md` 和 `形近词/*.md` 中的所有 `[[链接]]`
   - 目标在其他位置 → 修复路径
   - 目标不存在 → 标记 `[MISSING]`，报告给用户

2. **Frontmatter 完整性**：
   - 检查必要字段：`title`, `tags`, `mastery`, `word_freq`, `semantic_field`, `prototype`, `phonetic`, `pos`
   - 缺失 → 自动补全（如可能）

3. **级联一致性**：
   - 检查 Layer 1 单词笔记中引用的词根，是否在 Layer 2 有对应笔记
   - Layer 2 词根笔记的派生词族，是否包含所有引用该词根的 Layer 1 单词
   - 不一致 → 自动追加缺失条目

#### B. 启发式检查（仅报告）

4. **复习过期**：超过复习间隔 2 倍未复习的单词
5. **孤立笔记**：未被其他笔记 `[[链接]]` 的单词
6. **L0 堆积**：创建超过 7 天仍为 L0 的单词
7. **重复内容**：`prototype` 相同且 `semantic_field` 相同的笔记，建议合并
8. **知识库统计**：
   - 总词数、各语义场分布、mastery 分布
   - Layer 2 文件数（词根/同义辨析/形近词）
   - 平均 review_count

#### C. 自进化建议

9. **网络密度**：识别链接密集的"核心词"（被 3+ 其他笔记链接），建议优先深化
10. **薄弱区域**：识别单词数最少的语义场，建议针对性摄入
11. **复习效率**：识别 review_count 高但 mastery 仍低的单词，建议调整记忆策略

### 输出格式

```
## 巡检报告

### 自动修复（N 项）
- ✅ 修复 [[word1]] 的断链
- ✅ 补全 [[word2]] 缺失字段
- ✅ 级联更新 [[ambi]] 词根笔记：追加 2 个派生词

### 需要人工处理（M 项）
- ⚠️ [[word3]] 复习已过期 14 天
- ⚠️ [[word4]] 创建 21 天仍为 L0
- ⚠️ [[word5]] 与 [[word6]] 原型义相同，建议合并

### 知识库统计
- 总词数：42 | L0: 28 | L1: 10 | L2: 4
- 语义场：人本世界(15) 资本经济(12) 理性世界(8) ...
- 网络文件：词根(8) 同义辨析(5) 形近词(3)
- 薄弱区域：自然物理(0词) 物品工具(0词)
```

---

## 附录：HERMES 注释迁移记录

> 原模板中的 `%% HERMES-XX %%` 注释已全部移除，其功能说明迁入本节。AI 执行对应操作时，参考以下对照表。

| 原 HERMES 标记 | 功能说明 | 对应执行逻辑 |
|----------------|---------|-------------|
| `HERMES-INGEST` | 摄入时填充 frontmatter | Rule 1 Phase 2：按字段列表填充 |
| `HERMES-REVIEW` | 复习时更新记录 | Rule 4：更新 `last_review` + `review_count` |
| `HERMES-DEEPEN` | 深化时修正内容 | Rule 2：解析意图 → 修改 section |
| `HERMES-ACTIVATE` | 网络激活选择 | Rule 1 Phase 3.5：写入 `network_activation` frontmatter |
| `HERMES-CROSSREF` | 语义场双链一致性检查 | Rule 6：级联一致性检查 |
| `HERMES-TAIL-CHECK` | 词尾定性速查 | 填充时参考 [[词汇学习哲学#完整词尾分析规则]] |
| `HERMES-DUPLICATE` | 词根词缀与记忆锚点去重 | 填充时：若叙事重复，词根词缀简写"见记忆锚点" |
