# 全局格式与语料审查报告

## 结论

- 现有全量结果 `bad_count=0` 仅代表“基础结构与形式验收通过”
- 经过本轮 P1 修复后，仍有 `4862 / 5250` 个词条命中了至少一个高阶质量问题
- 当前全库的主要风险不再是旧模板结构，而是：
  - 模板化语料广泛存在
  - 主动产出区大量保留占位
  - 超纲词区存在系统性的标题命名漂移

## 质量维度

### 1. 结构合规率

- 依据上一轮全量修复与当前基础验收，结构性问题已基本清零
- 这仍然是当前库里质量最稳定的一层

### 2. 标签可信率

- 命中 `fake_exam_tag`：`0` 条，涉及 `0` 个文件
- 说明这一轮已经清掉了伪 `[真题]` 的高风险残留

### 3. 语料自然率

- 模板语料：`2311` 条，涉及 `985` 个文件
- 功能词误用：`0` 条，涉及 `0` 个文件
- 语义失配：`0` 条，涉及 `0` 个文件
- 说明高风险误导项已经清零，但模板化语料仍然是下一阶段的主问题

### 4. 占位清零率

- 主动产出占位残留：`4696` 个文件
- 说明大部分词条仍未形成真实用户产出内容

## 问题排序

### 数量最大

1. `output_placeholder_residue`：`4696`
2. `entry_n_template_corpus`：`2311`
3. `noncanonical_output_section_title`：`1050`

### 风险最高

1. `entry_n_template_corpus`：`2311`
2. `output_placeholder_residue`：`4696`
3. `noncanonical_output_section_title`：`1050`

## 样例证据

### 模板语料

- `ability.md`
  - `The report highlights the importance of ability in the final decision. [例]`
  - `The committee discussed the latest ability during the meeting. [例]`
  - `The article examines how ability can shape public policy. [例]`

### P1 修复结果

- `fake_exam_tag`：已清零
- `function_word_invalid_usage`：已清零
- `semantic_mismatch`：已清零

### 标题漂移

- `Broadway.md`
  - `> [!success]- 内化标记`

## 审查判断

- 当前仓库已经不需要再把主要精力放在“旧模板结构修复”上
- 下一阶段应转向：
  - 模板句替换
  - 主动产出区规范化
  - 超纲词主动产出区标题统一

## 建议的后续执行顺序

1. 继续清理 `P2`
2. 再统一规范 `P3`

## 对当前仓库状态的总体评价

- 结构层：良好
- 基础格式层：良好
- 来源可信度层：P1 已收口
- 语料自然度层：需系统优化
- 主动产出完整度层：明显不足
