---
title: "词汇仪表盘"
tags:
  - 学习/英语/词汇
  - 仪表盘
date: 2026-04-20
---

# 词汇仪表盘

> [!tip] 使用说明
> 本仪表盘基于 Obsidian Dataview 插件实现实时查询。

---

## 知识库规模总览

```dataview
TABLE length(rows) AS 笔记数
FROM "VocabVault/Wiki"
WHERE file.name != "_模板-单词笔记"
GROUP BY file.folder
SORT length(rows) DESC
```

## 各语义场词数统计

```dataview
TABLE length(rows) AS 词数
FROM "VocabVault/Wiki/L0_单词集合"
WHERE semantic_field
GROUP BY semantic_field
SORT length(rows) DESC
```

## 掌握程度分布

```dataview
TABLE word_freq, prototype AS 原型义
FROM "VocabVault/Wiki/L0_单词集合"
WHERE mastery
SORT mastery ASC
```

## 按词频筛选

### 高频词

```dataview
LIST prototype AS 原型义
FROM "VocabVault/Wiki/L0_单词集合"
WHERE word_freq = "高频"
SORT file.name ASC
```

### 中频词

```dataview
LIST prototype AS 原型义
FROM "VocabVault/Wiki/L0_单词集合"
WHERE word_freq = "中频"
SORT file.name ASC
```

## 待复习词（L0-L1）

```dataview
LIST prototype AS 原型义, extension_dim AS 延伸维度
FROM "VocabVault/Wiki/L0_单词集合"
WHERE mastery = "L0" OR mastery = "L1"
SORT file.name ASC
```

## 最近添加的词汇

```dataview
TABLE word_freq AS 词频, semantic_field AS 语义场, prototype AS 原型义
FROM "VocabVault/Wiki/L0_单词集合"
SORT date DESC
LIMIT 20
```
