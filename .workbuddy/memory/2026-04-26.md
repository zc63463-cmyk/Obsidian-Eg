# 2026-04-26

## 单词模板优化

- 重写了 `Wiki/_模板-单词笔记.md`，核心变更：
  - Frontmatter 新增 5 字段：phonetic, pos, metaphor_type, word_root, network_activation
  - 移除所有 HERMES 注释（15+ 行/篇）
  - 词尾定性速查从 callout 精简为一行链接引用
  - 复习记录移除 4 行 checkbox 模板
  - 网络激活 section 整体删除，迁入 frontmatter
  - 词义链路 callout 从 [!tip] 改为 [!abstract]
  - 词根词缀叙事与记忆锚点去重规则：若重复则词根词缀写"见记忆锚点"

- 创建了 `Wiki/_模板修正规范.md`，供 Trae 批量修正 1500+ 笔记使用
  - 包含 5 大修正类别：Frontmatter 新增、语义场一致性、HERMES 移除、正文精简、callout 统一
  - 含完整的 abandon.md 修正前后对比示例
  - 推荐执行顺序和注意事项

- 尚未执行的任务（3-5）：更新指令实现.md、更新交互指令.md、修复 Dataview 路径

## 基础设施文件更新

- 更新了 `指令实现.md`：
  - Frontmatter 字段列表新增 5 字段
  - 保存路径从 `VocabVault/Wiki/L0_单词集合/` 修正为 `Wiki/L0_单词集合/`
  - 网络激活逻辑从"勾选 checkbox"改为"写入 frontmatter network_activation"
  - 巡检必要字段列表更新
  - 末尾新增"HERMES 注释迁移记录"附录，7 个 HERMES 标记的功能说明迁入

- 更新了 `交互指令.md`：
  - 设计哲学 callout 中网络激活说明补充 frontmatter 方式
  - 网络激活选项说明从 checkbox 改为 frontmatter network_activation

- 修复了所有 Dataview 路径（共 13 个文件）：
  - `词汇仪表盘.md`：6 处 `VocabVault/Wiki` → `Wiki`
  - `词汇知识库索引.md`：4 处路径修正
  - `Wiki/语义场/*.md`：10 个文件各 1 处路径修正
  - 验证：基础设施文件无残留 VocabVault 路径引用
  - L0_单词集合中残留的 VocabVault 仅在 HERMES 注释中，将在批量修正时移除

## 知识库框架发展规划讨论

- 用户询问后续知识库框架如何构建，当前只有单词笔记 + 词根词缀笔记
- 知识库是 vocab-observatory.vercel.app 的数据源（Obsidian → GitHub → Supabase → Vercel）
- 网站当前 0 词条可见，数据管道未跑通
- 引用了 Karpathy LLM Wiki 模式做对标分析
- 核心发现：体系有 L0→L4 三层设计但只有 L0 有实质数据，同义辨析和形近词目录为空
- 最大差距：缺乏"知识复利"机制——新词摄入不级联更新其他页面
- 提出了 4 阶段发展路线：夯实基础 → 织网 → 知识复利引擎 → 网站 2.0
- 优先级：P0 调试网站管道 + 批量修正，P1 词根双向链接 + 同义辨析生成

## vocab-observatory P0 探索

- Clone 项目到 `E:\Projects\vocab-observatory`（原本地无此仓库）
- **数据管道诊断结论**：网站数据管道已跑通，实际有 **1000 词条**正常工作
  - 首页 "0 词条" 是误判：AnimatedCounter 是客户端组件，初始 display=0，web_fetch 抓取 SSR HTML 时动画未启动
  - `/words` 页面正常显示 120 条（截断），API 确认 total=1000
  - Featured 区域有 6 个词条正常展示
  - 数据管道：GitHub Archive → JSZip 解压 → parseMarkdown 解析 → Supabase upsert → ISR 缓存
- **Breadcrumb.tsx 类型修复**：
  - 问题：`href?: Route<string>` 对 Next.js 16 的路由类型强耦合，动态路径传值会报错
  - 方案：对外接口保持 `href?: string`（调用者友好），内部通过 `as Route` 断言适配 Link
  - 同时修复了 `plaza/[slug]/page.tsx` 中 `redirect()` 的 Route 类型断言
  - tsc 和 eslint 均通过

## Trae 批量修正文档生成

- 生成了两份文件供 Trae 网页版 Solo MTC 使用：
  - `Wiki/_系统/_Trae批量修正规范.md`：9 项修正的详细匹配规则、提取逻辑、替换操作、边界情况
  - `Wiki/_系统/_Trae批量修正指令.md`：主指令 + 分批指令 + 验证指令
- 三个目标目录：L0_单词集合(1793)、L0_基础词(2406)、L0_超纲词(1050)，共约 5249 篇
- 建议每批 200 篇执行
- 修正 7（词根去重）是最复杂的，需语义比较，不确定时保留原文

## Wiki 文件整理

- 将 4 个 `_` 前缀的模板/规范文件从 `Wiki/` 根目录移入 `Wiki/_系统/`：
  - `_模板-单词笔记.md`
  - `_模板修正规范.md`
  - `_Trae批量修正规范.md`
  - `_Trae批量修正指令.md`
- 已更新所有内部路径引用（4 处），无残留旧路径

## word_freq 值更新

- **字段名不变**：保持 `word_freq`，仅改值
- **值映射**：`必备词`(L0_单词集合) / `基础词`(L0_基础词) / `超纲词`(L0_超纲词)
- 改动范围：5 个文件（模板、修正规范、Trae 规范/指令、词汇仪表盘）
- 新增 Trae 批量修正项：修正 1b（word_freq 值按目录自动确定）
- 已推送至 GitHub (commit `1121c7d`)

## Git Pull — 批量修正已合并

- `git pull origin main` 成功：`1121c7db..213b5ec8` (Fast-forward)
- 最新提交：`feat: 批量修正5249个单词笔记模板格式`
- 变更：5,249 个文件，+47,219 / -136,618（模板精简净减约 9 万行）
- 本地与远程已同步

## 单词卡 Raw 文件质量评估

- 从 Obsidian / Karpathy LLM Wiki / NotebookLM 三个视角评估
- **核心结论**：认知框架和结构化程度达标，短板在卡间网络
- **P0 问题**：word_root 字段空值（图查询断链）、语义场 Dataview 只查 L0_单词集合（漏 3456 词）
- **P1 问题**：同义辨析/形近词目录只有模板无内容、network_activation 全空
- **P2 问题**：主动产出空壳占位符噪声、NotebookLM 需清洗版（去掉 wikilink/callout 语法）
- **架构建议**：Karpathy 模式分层——Raw（当前单词卡不可变）→ Wiki（LLM 维护的聚合页）→ Schema（_系统/ 目录）

## Wiki 层重建策略讨论

- 用户问：应该让 Trae 重写 raw 卡再提取，还是直接提取？
- **结论：直接提取 + 定点修补 raw 字段，不重写 raw 内容**
- 词根页面质量三档：A级(4-5KB, ~15个, 有含义延伸链路) / B级(2.5-4KB, ~55个) / C级(1-2.5KB, ~30个)
- 语义场页面全是空壳，仅 Dataview 查询
- 形近字和同义辨析页面完全空白
- 执行计划：B0(word_root回填) → B1(C级词根重写) → B2(B级词根升级) → B3(语义场升级) → B4(形近字生成) → B5(双向链接确认)

## B0 回填全库校验

- git pull 成功：Trae 已执行 B0 回填，5222 文件变更 + 校验信息目录
- 全库扫描结果（5249 文件）：
  - word_root 精确匹配词根目录：810 (15%)
  - word_root 可通过映射表修正：206 (3%)
  - word_root 短词根但无映射：~2965 (56%) — 多为合法词根形素但词根目录无页面
  - word_root 长值/错误提取：~637 (12%) — 同源词/形近词/完整词被误当词根
  - word_root 为空但 na 含"词根"：623 (12%)
- **根因**：B0 提取逻辑取了正文中第一个 `[[wikilink]]`，而非词根本身
- 错误模式：同源词当词根(receipt→receive)、形近词当词根(though→thought)、前缀派生词当词根(category→catastrophe)、复合词成分当词根(workforce→work)
- 构建了词根规范化映射表（221 条，覆盖 100 个规范词根名）
- 写了修正方案：Phase 1 机械修正(A修正+长值清空+映射规范化) + Phase 2 LLM 重新提取
- 产出文件：
  - `Wiki/_校验信息/B0回填全库校验报告-2026-04-26.md`
  - `Wiki/_系统/_Trae-B0-Raw字段回填规范.md` 已追加第六节修正方案

## Wiki 层重建指令文件生成

- 创建了 5 个 Trae 指令/规范文件，存放于 `Wiki/_系统/`：
  - `_Trae-B0-Raw字段回填规范.md`：word_root 从正文 wikilink/词源关联提取 + network_activation 程序化判定
  - `_Trae-B1B2-词根页面升级规范.md`：词根页面质量分 A/B/C 三级，含完整重写示例(techn.md)和含义延伸链路格式
  - `_Trae-B3-语义场页面升级规范.md`：新增场内共性分析/子分类/跨场关联，Dataview 扩展到三目录
  - `_Trae-B4-形近字生成规范.md`：四类判定逻辑(前缀后缀互换>词根变体>拼写相似>音近误混)，含预判高频组清单
  - `_Trae-Wiki层重建主指令.md`：B0→B5 主调度文件，每批次含完整 Trae prompt

## B0 Phase 1 机械修正（已推送 commit 7ca418d1）

- 修正 966 个文件（967 含校验报告新增文件）：
  - 1a: 623 个 word_root 为空但 na 含"词根" → 从 na 移除"词根"
  - 1b: 137 个 word_root 长值/错误提取（>8字符无连字符） → 清空 word_root + 移除 na 中的"词根"
  - 1c: 199 个 word_root 可通过映射表规范化 → 替换为规范名（如 mit→miss-mit, ced→ced-cess）
  - 额外修复 7 个 word_root 是规范名但 na 缺"词根"的文件
- 修正后全库验证：Problem A=0, Problem B=0, word_root 精确匹配词根目录=1009(+199)
- 剩余 3472 个"短词根无映射"文件留给 Phase 2（LLM 重新提取）
- **踩坑**：正则 `\s*` 会跨行匹配导致 `word_root:` 后空值时吞掉下一行 `network_activation`，已改用逐行解析避免

## 单词模板合理性检测与修订

- 用 English Word Coach 视角检测了模板，发现 7 个问题
- 深入探讨了问题 3/4/6/7：
  - 问题 3（搭配缺句型框架）→ 在核心释义嵌入 `V N` / `V that-clause` 等句型标注
  - 问题 4（语料太薄弱）→ 三级可信度体系 `[真题]`/`[COCA]`/`[例]`，要求完整句子
  - 问题 6 搁置，改用 FSRS 标准记忆模型
  - 问题 7（5+3 维度框架）→ 确认够用，用子标题无限扩展
- 产出文件：
  - `Wiki/_系统/_延伸维度与隐喻类型参考.md` — 5 延伸维度 + 3 隐喻类型，含子标题细分
  - `Wiki/_系统/_模板修订方案-句型与语料.md` — 问题 3/4 的完整修订方案
- 5 个示范词修订完成：abandon, complete, suggest, oath, adopt
- 模板文件 `_模板-单词笔记.md` 已更新核心释义/搭配/语料三个 section
